# MVP Defect Batch - 2026-06-13

**Session ID:** conv-b6135571-b667-4e18-8d6b-2d60c6706257  
**Build:** 1.0.0  
**Tester:** User  
**Environment:** Remote Windows + Gemini + SQL Server  
**Session Date:** 2026-06-13 (11:07 - 11:36 UTC)  
**Prepared At:** 11:36  

## Summary

- **Total reviewed cases:** 15+ requests
- **Correct:** 5 (simple list queries, schema lookups)
- **Partially-correct:** 3 (tool-call loop but data returned)
- **Incorrect:** 7 (provider errors, schema mismatches, timeout)
- **P0 failures:** 2 (Gemini 502/504 stream termination)

## Root Cause Groups

### PROVIDER (High Impact)

**Cases:**
- cc390a4b-7165-4ee4-96da-b25b804bf2da (stream terminated, retry=3)
- cea14a32-0471-4848-a9af-0f980baa970d (502 Bad Gateway, requestId=3c5386ff2e4895cd019677a215fab2e2)
- 1b21a6c6-7d7d-4512-bdeb-cf1ac44d8310 (timeout/no response)

**Shared symptom:**
- Gemini client encounters network error during streaming
- Retry logic exhausts (3 attempts max)
- Error message is garbled (encoding issue in telemetry)
- Takes 20-110 seconds to fail (no fast-fail pattern)

**Proposed fix:**
1. Add fast-fail after first upstream 5xx response (no retry cascade)
2. Log raw upstream response code + headers to telemetry (not HTML body)
3. Timeout socket read at 10s per chunk to avoid long hangs
4. Add circuit breaker: if 3 consecutive 502/504, mark provider unhealthy for 60s

**Regression tests:**
- Mock Gemini 502 response, verify fail-fast in < 1s
- Mock streaming timeout at 5s into response, verify graceful abort
- Verify telemetry contains clean error code, not HTML garbage

---

### INTENT (Medium Impact)

**Cases:**
- 42cfe149-1159-493d-ad3e-00454cab67df (loop exceeded at round 5)
- 1371a5ba-ec18-4a40-a563-e1adf5b6e374 (loop exceeded at round 5)
- 9e042dc1-3406-451d-b3e6-2f13d938f1b4 (retry from LAG error)

**Shared symptom:**
- Questions are multi-step financial queries (sales, invoices, fiscal year joins)
- Agent makes correct tool calls but SQL response is not actionable
- Loop limit reached before final query succeeds
- Loop count: 3-5 tool calls before limit
- Round sequence: list_tables → get_schema (×2-3) → fetch_financial_data → error → restart

**Proposed fix:**
1. Increase tool-call limit from 5 to 8 (or make configurable)
2. Add caching for schema lookups within same conversation (avoid re-fetch)
3. Detect multi-table query pattern early and batch schema retrieval
4. Add "question too complex for automatic routing, suggest simplification" hint after 3 loops

**Regression tests:**
- Benchmark question with 2-table join should complete in ≤ 3 loops
- 5-table query should give helpful suggestion instead of timeout
- Schema cache should skip redundant get_database_schema calls for same table

---

### SCHEMA (Medium Impact)

**Cases:**
- 42cfe149-1159-493d-ad3e-00454cab67df (Invalid column name 'Name')
- 9e042dc1-3406-451d-b3e6-2f13d938f1b4 (LAG not recognized)

**Shared symptom:**
- Column name generated by LLM does not match actual schema (e.g., 'Name' vs actual column 'Title')
- SQL function used is not available in SQL Server version (LAG, window functions)
- Schema retrieved correctly but context window truncation loses column names
- Error occurs after tool loop advances, no retry path

**Proposed fix:**
1. In fetch_financial_data, validate column names against schema before execute
2. Add SQL Server 2019+ compatibility check: disallow window functions (LAG, LEAD) if not supported
3. Augment get_database_schema response with "common_aliases" hint (e.g., "Name / Title both refer to description")
4. If validation fails, return corrected suggestion to agent instead of raw SQL error

**Regression tests:**
- Query with wrong column name should return "suggested column: X" instead of EREQUEST
- Window function query should return "unsupported in SQL Server 2019, use alternative" message

---

### SQL_POLICY (Already Managed)

**Cases:**
- None in this batch (policy enforcement working correctly)

**Note:** Previous batch fixed TOP/ORDER BY requirements; no violations in latest batch.

---

## Batch Recommendation

- **Ship now / wait for more evidence:** Wait for PROVIDER fix (2-3 days), then ship
- **Suggested owner:** 
  - PROVIDER fast-fail: @backend-team (GeminiClient retry logic)
  - INTENT loop tuning: @orchestrator-team (AgentOrchestrator limit + caching)
  - SCHEMA validation: @api-contract-team (fetch_financial_data pre-flight check)
- **Target batch date:** 2026-06-16 (early adopter testing for PROVIDER, INTENT cache by 2026-06-17)

---

## Notes

- Telemetry is working well; all errors captured and logged to collector successfully.
- No data loss; queue drains correctly on app restart.
- Test batch revealed realistic complex queries; loop limit and schema mismatches are not edge cases.
